[build-system]
requires = ["setuptools>=57.0"]
build-backend = "setuptools.build_meta"

[project]
name = "helloworld"
version = "0.4.0"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["pipelines*"]